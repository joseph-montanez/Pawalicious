Pawalicious is proof of concept that web development can be done in Vala. It is heavily reliant on libsoup for serving generated content.

required ubutu libraries:
sudo apt-get install libgnet-dev libmysqlclient-dev libjson-glib-dev
